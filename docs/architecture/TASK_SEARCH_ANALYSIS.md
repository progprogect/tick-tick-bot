# Анализ проблемы поиска задач и архитектурное решение

**Дата:** 2024-11-04  
**Статус:** Анализ завершен, решение предложено

---

## 🔍 Анализ текущей ситуации

### Проблема

**Симптом:** Запрос "поставь дедлайн к задаче протестировать работу завтра 8:00" не находит существующую задачу.

**Корневая причина:** 
1. Кэш задач пустой (задачи созданы не через бота)
2. Поиск в API использует неточное сравнение
3. Отсутствует единая стратегия получения актуальных данных о задачах

---

## 📊 Текущая логика поиска задач

### 1. TaskManager.update_task()

```python
# Шаг 1: Поиск в кэше
task_id = self.cache.get_task_id_by_title(title=command.title, project_id=command.project_id)

# Шаг 2: Если не найдено - поиск в API
if not task_id:
    tasks = await self.client.get_tasks()  # ❌ Получает ВСЕ задачи из ВСЕХ проектов
    matching_task = next(
        (t for t in tasks if t.get("title") == command.title),  # ❌ Точное сравнение без нормализации
        None
    )
```

**Проблемы:**
- ❌ `get_tasks()` без параметров получает задачи из всех проектов (медленно)
- ❌ Сравнение `t.get("title") == command.title` не учитывает регистр и пробелы
- ❌ Нет частичного совпадения (fuzzy matching)

### 2. SmartRouter._resolve_task_identifier()

```python
# Та же проблема
tasks = await self.client.get_tasks()  # ❌ Все проекты
matching_task = next(
    (t for t in tasks if t.get("title") == identifier.value),  # ❌ Точное сравнение
    None
)
```

### 3. TagManager.add_tags() и NoteManager.add_note()

```python
# Та же проблема
tasks = await self.client.get_tasks()  # ❌ Все проекты
matching_task = next(
    (t for t in tasks if t.get("title") == command.title),  # ❌ Точное сравнение
    None
)
```

### 4. ReminderManager.set_reminder()

```python
# Та же проблема
tasks = await self.client.get_tasks()  # ❌ Все проекты
matching_task = next(
    (t for t in tasks if t.get("title") == command.title),  # ❌ Точное сравнение
    None
)
```

### 5. DataFetcher.fetch_task_by_title()

```python
# ✅ Хорошо: использует нормализацию
tasks = await self.client.get_tasks(project_id=project_id)  # ✅ С project_id
matching_task = next(
    (t for t in tasks if normalize_title(t.get("title", "")) == search_title_normalized),  # ✅ Нормализация
    None
)
```

**Этот метод работает правильно!** Но он используется только в multi-stage процессе GPT.

---

## 🐛 Найденные проблемы

### Проблема 1: Кэш пустой

**Причина:** 
- Кэш заполняется только при создании задач через бота
- Задачи, созданные вручную в TickTick, не попадают в кэш

**Влияние:**
- Все поиски по названию падают на этап поиска в API
- Поиск в API неэффективен и неточен

---

### Проблема 2: Неточное сравнение названий

**Текущий код:**
```python
t.get("title") == command.title  # ❌ Точное сравнение
```

**Проблемы:**
- Не учитывает регистр: "Протестировать работу" ≠ "протестировать работу"
- Не учитывает пробелы: "протестировать  работу" ≠ "протестировать работу"
- Не учитывает частичное совпадение

**Правильный подход (как в DataFetcher):**
```python
def normalize_title(t: str) -> str:
    if not t:
        return ""
    import re
    return re.sub(r'\s+', ' ', t.lower().strip())

normalize_title(t.get("title", "")) == normalize_title(command.title)  # ✅
```

---

### Проблема 3: Неэффективный поиск в API

**Текущий код:**
```python
tasks = await self.client.get_tasks()  # ❌ Без project_id
```

**Проблемы:**
- Получает задачи из ВСЕХ проектов (медленно)
- Если у пользователя много проектов, это может быть очень медленно
- Не использует кэш проектов

**Правильный подход:**
1. Получить список проектов (кэшировать)
2. Для каждого проекта получить задачи через `GET /open/v1/project/{projectId}/data`
3. Искать в полученном списке

---

### Проблема 4: Отсутствие project_id при обновлении

**Согласно API документации:**
> При обновлении задачи через `POST /open/v1/task/{taskId}` **ОБЯЗАТЕЛЬНО** нужно передавать в теле запроса `id` и `projectId`.

**Текущая проблема:**
- `project_id` не всегда известен
- Если задача не в кэше, `project_id` неизвестен
- Приходится искать задачу, чтобы получить `project_id`

---

### Проблема 5: Дублирование логики поиска

**Проблема:**
- Логика поиска дублируется в 5+ местах:
  - `TaskManager.update_task()`
  - `TaskManager.delete_task()`
  - `SmartRouter._resolve_task_identifier()`
  - `TagManager.add_tags()`
  - `NoteManager.add_note()`
  - `ReminderManager.set_reminder()`

**Последствия:**
- Изменения нужно вносить в нескольких местах
- Легко забыть обновить одно из мест
- Разные версии логики в разных местах

---

## 🏗️ Архитектурное решение

### Принципы решения

1. **Единая точка поиска задач** — один сервис для всех поисков
2. **Кэширование проектов** — список проектов кэшируется и обновляется при необходимости
3. **Нормализация названий** — единая функция нормализации для всех сравнений
4. **Умный поиск** — сначала кэш, потом API с нормализацией и частичным совпадением
5. **Синхронизация кэша** — при поиске задачи в API автоматически добавлять в кэш

---

### Решение 1: Создать TaskSearchService

**Назначение:** Единый сервис для поиска задач по названию.

**Методы:**
```python
class TaskSearchService:
    async def find_task_by_title(
        self,
        title: str,
        project_id: Optional[str] = None,
        use_cache: bool = True,
        use_api: bool = True,
    ) -> Optional[Dict[str, Any]]:
        """
        Найти задачу по названию
        
        Алгоритм:
        1. Если use_cache=True: поиск в кэше (с нормализацией)
        2. Если не найдено и use_api=True: поиск в API
           - Если project_id указан: поиск только в этом проекте
           - Если project_id не указан: поиск во всех проектах
        3. При нахождении в API: автоматически добавить в кэш
        4. Вернуть найденную задачу или None
        """
        
    async def find_task_id_by_title(
        self,
        title: str,
        project_id: Optional[str] = None,
    ) -> Optional[str]:
        """
        Найти task_id по названию (удобный метод)
        """
        task = await self.find_task_by_title(title, project_id)
        return task.get("id") if task else None
```

**Преимущества:**
- ✅ Единая логика поиска
- ✅ Нормализация названий
- ✅ Частичное совпадение
- ✅ Автоматическое кэширование
- ✅ Оптимизация (поиск только в нужном проекте)

---

### Решение 2: Кэширование проектов

**Назначение:** Кэшировать список проектов, чтобы не запрашивать каждый раз.

**Реализация:**
```python
class ProjectCacheService:
    def __init__(self):
        self._projects: List[Dict[str, Any]] = []
        self._last_update: Optional[datetime] = None
        self._cache_ttl: timedelta = timedelta(minutes=5)  # 5 минут
    
    async def get_projects(self, force_refresh: bool = False) -> List[Dict[str, Any]]:
        """
        Получить список проектов (с кэшированием)
        
        Args:
            force_refresh: Принудительно обновить кэш
        
        Returns:
            Список проектов
        """
        if force_refresh or self._should_refresh():
            self._projects = await self.client.get_projects()
            self._last_update = datetime.now()
        
        return self._projects
    
    def _should_refresh(self) -> bool:
        """Проверить, нужно ли обновить кэш"""
        if not self._projects or not self._last_update:
            return True
        return datetime.now() - self._last_update > self._cache_ttl
```

**Преимущества:**
- ✅ Меньше запросов к API
- ✅ Быстрее получение списка проектов
- ✅ Автоматическое обновление при необходимости

---

### Решение 3: Улучшить TaskCacheService

**Добавить методы:**
```python
class TaskCacheService:
    async def sync_task_from_api(
        self,
        task_id: str,
        project_id: str,
        client: TickTickClient,
    ) -> None:
        """
        Синхронизировать задачу из API в кэш
        
        Получает актуальные данные задачи из API и сохраняет в кэш
        """
        try:
            task = await client.get(
                endpoint=f"/open/v1/project/{project_id}/task/{task_id}",
                headers=client._get_headers(),
            )
            if isinstance(task, dict):
                self.save_task(
                    task_id=task.get("id"),
                    title=task.get("title"),
                    project_id=task.get("projectId"),
                    status="completed" if task.get("status") == 2 else "active",
                    tags=task.get("tags", []),
                    notes=task.get("content", ""),
                    reminders=task.get("reminders", []),
                    repeat_flag=task.get("repeatFlag"),
                )
        except Exception as e:
            self.logger.warning(f"Failed to sync task {task_id} from API: {e}")
    
    async def bulk_sync_tasks_from_project(
        self,
        project_id: str,
        client: TickTickClient,
    ) -> int:
        """
        Синхронизировать все задачи из проекта в кэш
        
        Returns:
            Количество синхронизированных задач
        """
        try:
            response = await client.get(
                endpoint=f"/open/v1/project/{project_id}/data",
                headers=client._get_headers(),
            )
            if isinstance(response, dict) and "tasks" in response:
                tasks = response["tasks"]
                count = 0
                for task in tasks:
                    self.save_task(
                        task_id=task.get("id"),
                        title=task.get("title"),
                        project_id=task.get("projectId"),
                        status="active",  # GET /data возвращает только незавершенные
                        tags=task.get("tags", []),
                        notes=task.get("content", ""),
                        reminders=task.get("reminders", []),
                        repeat_flag=task.get("repeatFlag"),
                    )
                    count += 1
                return count
        except Exception as e:
            self.logger.warning(f"Failed to sync tasks from project {project_id}: {e}")
        return 0
```

**Преимущества:**
- ✅ Автоматическая синхронизация задач из API
- ✅ Кэш всегда актуален
- ✅ Можно синхронизировать весь проект сразу

---

### Решение 4: Рефакторинг всех сервисов

**Заменить дублированный код на использование TaskSearchService:**

**Было:**
```python
# TaskManager.update_task()
task_id = self.cache.get_task_id_by_title(title=command.title)
if not task_id:
    tasks = await self.client.get_tasks()
    matching_task = next((t for t in tasks if t.get("title") == command.title), None)
    if matching_task:
        task_id = matching_task.get("id")
```

**Станет:**
```python
# TaskManager.update_task()
task_search = TaskSearchService(self.client, self.cache)
task = await task_search.find_task_by_title(command.title, command.project_id)
if task:
    command.task_id = task.get("id")
else:
    raise ValueError(f"Задача '{command.title}' не найдена")
```

**Применить к:**
- ✅ `TaskManager.update_task()`
- ✅ `TaskManager.delete_task()`
- ✅ `SmartRouter._resolve_task_identifier()`
- ✅ `TagManager.add_tags()`
- ✅ `NoteManager.add_note()`
- ✅ `ReminderManager.set_reminder()`

---

## 📋 План реализации

### Этап 1: Создать TaskSearchService

1. Создать файл `src/services/task_search_service.py`
2. Реализовать методы поиска с нормализацией
3. Добавить автоматическое кэширование

### Этап 2: Создать ProjectCacheService

1. Создать файл `src/services/project_cache_service.py`
2. Реализовать кэширование проектов с TTL

### Этап 3: Улучшить TaskCacheService

1. Добавить методы синхронизации из API
2. Добавить метод массовой синхронизации проекта

### Этап 4: Рефакторинг сервисов

1. Заменить дублированный код в `TaskManager`
2. Заменить дублированный код в `SmartRouter`
3. Заменить дублированный код в `TagManager`
4. Заменить дублированный код в `NoteManager`
5. Заменить дублированный код в `ReminderManager`

### Этап 5: Тестирование

1. Протестировать поиск задач, созданных через бота
2. Протестировать поиск задач, созданных вручную
3. Протестировать поиск с разными вариантами названий
4. Протестировать производительность

---

## 🎯 Ожидаемые результаты

### После реализации:

1. ✅ **Поиск работает для всех задач** (созданных через бота и вручную)
2. ✅ **Нормализация названий** (регистр, пробелы не важны)
3. ✅ **Частичное совпадение** (находит "протестировать работу" по запросу "протестировать")
4. ✅ **Быстрый поиск** (кэш проектов, поиск только в нужном проекте)
5. ✅ **Единая логика** (один сервис для всех поисков)
6. ✅ **Автоматическое кэширование** (найденные задачи автоматически попадают в кэш)

---

## 🔧 Технические детали

### Нормализация названий

```python
def normalize_title(title: str) -> str:
    """
    Нормализовать название задачи для сравнения
    
    Правила:
    - Привести к нижнему регистру
    - Убрать лишние пробелы
    - Заменить множественные пробелы одним
    """
    if not title:
        return ""
    import re
    return re.sub(r'\s+', ' ', title.lower().strip())
```

### Частичное совпадение

```python
def find_partial_match(search_title: str, task_titles: List[str]) -> Optional[str]:
    """
    Найти частичное совпадение
    
    Алгоритм:
    1. Проверить, содержится ли search_title в task_title
    2. Проверить, содержится ли task_title в search_title
    3. Выбрать самое длинное совпадение (более специфичное)
    """
    search_normalized = normalize_title(search_title)
    matches = []
    
    for task_title in task_titles:
        task_normalized = normalize_title(task_title)
        if (search_normalized in task_normalized or 
            task_normalized in search_normalized):
            matches.append((task_title, len(task_normalized)))
    
    if matches:
        # Выбрать самое длинное совпадение
        matches.sort(key=lambda x: x[1], reverse=True)
        return matches[0][0]
    
    return None
```

---

## 📝 Заметки по реализации

### Важные моменты:

1. **Обратная совместимость:** Старые методы должны продолжать работать
2. **Логирование:** Все поиски должны логироваться для отладки
3. **Обработка ошибок:** Graceful degradation при ошибках API
4. **Производительность:** Кэш должен уменьшать количество запросов к API

### Риски:

1. **Производительность:** Поиск во всех проектах может быть медленным
   - **Митигация:** Кэш проектов, поиск только в нужном проекте
2. **Синхронизация:** Кэш может быть неактуальным
   - **Митигация:** TTL для кэша проектов, синхронизация при поиске
3. **Множественные совпадения:** Несколько задач с похожими названиями
   - **Митигация:** Выбирать самое длинное совпадение, логировать предупреждения

---

## ✅ Критерии успеха

1. ✅ Запрос "поставь дедлайн к задаче протестировать работу завтра 8:00" находит задачу
2. ✅ Поиск работает для задач, созданных вручную в TickTick
3. ✅ Поиск работает с разными вариантами названий (регистр, пробелы)
4. ✅ Поиск работает быстро (кэш проектов)
5. ✅ Все сервисы используют единую логику поиска

---

**Дата создания:** 2024-11-04  
**Статус:** Готово к реализации

