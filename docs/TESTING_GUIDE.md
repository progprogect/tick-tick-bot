# Инструкция по тестированию проекта
## Telegram Bot для управления TickTick

**Дата:** 2024-11-04  
**Версия:** 1.0

---

## Оглавление

1. [Подготовка к тестированию](#подготовка-к-тестированию)
2. [Запуск проекта](#запуск-проекта)
3. [Тестирование через веб-интерфейс](#тестирование-через-веб-интерфейс)
4. [Тестирование основных функций](#тестирование-основных-функций)
5. [Проверка логов](#проверка-логов)
6. [Ожидаемые результаты](#ожидаемые-результаты)

---

## Подготовка к тестированию

### 1. Проверка зависимостей

Убедитесь, что установлены все зависимости:

```bash
cd /Users/mikitavalkunovich/Desktop/Cursor/Tick-tick
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Проверка конфигурации

Убедитесь, что в `.env` файле (или в `docs/credentials.md`) указаны:

- **Telegram Bot Token**: `TELEGRAM_BOT_TOKEN`
- **OpenAI API Key**: `OPENAI_API_KEY`
- **TickTick API Token**: `TICKTICK_ACCESS_TOKEN`
- **TickTick Email**: `TICKTICK_EMAIL` (опционально)
- **TickTick Password**: `TICKTICK_PASSWORD` (опционально)

Если файл `.env` не существует, создайте его на основе `docs/credentials.md`:

```bash
cp docs/credentials.md .env
# Затем отредактируйте .env и оставьте только переменные окружения
```

### 3. Проверка структуры проекта

Убедитесь, что существует файл кэша задач:

```bash
ls -la task_cache.json
# Если файла нет, он создастся автоматически при первом создании задачи
```

---

## Запуск проекта

### Вариант 1: Запуск веб-сервера для тестирования

```bash
cd /Users/mikitavalkunovich/Desktop/Cursor/Tick-tick
source venv/bin/activate
python3 -m uvicorn src.web.main:app --host 127.0.0.1 --port 8000 --reload
```

Веб-сервер будет доступен по адресу: **http://127.0.0.1:8000**

### Вариант 2: Запуск в фоновом режиме

```bash
cd /Users/mikitavalkunovich/Desktop/Cursor/Tick-tick
source venv/bin/activate
python3 -m uvicorn src.web.main:app --host 127.0.0.1 --port 8000 > /tmp/ticktick_bot.log 2>&1 &
echo $! > /tmp/ticktick_bot.pid
```

Остановка фонового процесса:

```bash
kill $(cat /tmp/ticktick_bot.pid)
```

---

## Тестирование через веб-интерфейс

### 1. Откройте веб-интерфейс

Откройте в браузере: **http://127.0.0.1:8000**

Вы увидите интерфейс с:
- Поле для ввода текстовой команды
- Кнопка "Отправить команду"
- Примеры команд
- Статус сервиса

### 2. Проверка статуса

Внизу страницы должно быть написано: **✅ Сервис работает**

Если статус другой, проверьте логи:

```bash
tail -f /tmp/ticktick_bot.log
```

---

## Тестирование основных функций

### Тест 1: Создание задачи (AC-2) ✅

**Команда:**
```
Создай задачу купить молоко
```

**Ожидаемый результат:**
```
✓ Задача 'купить молоко' создана в списке inbox129688987
```

**Что проверить:**
- ✅ Задача создана в TickTick
- ✅ Сообщение появилось в течение 3-5 секунд
- ✅ Задача сохранена в кэш (проверьте `task_cache.json`)

**Проверка кэша:**
```bash
cat task_cache.json | jq .
```

---

### Тест 2: Редактирование задачи (AC-3) ✅

**Команда:**
```
Измени задачу купить молоко на завтра
```

**Ожидаемый результат:**
```
✓ Задача 'купить молоко' обновлена: дата изменена на 05.11.2024
```

**Что проверить:**
- ✅ Задача обновлена в TickTick
- ✅ Дата изменена на завтра
- ✅ Сообщение показывает конкретные изменения

**Альтернативные команды:**
- `Измени задачу купить молоко на высокий приоритет`
- `Измени название задачи купить молоко на купить молоко и хлеб`

---

### Тест 3: Добавление тегов (AC-7) ✅

**Команда:**
```
Добавь тег важное к задаче купить молоко
```

**Ожидаемый результат:**
```
✓ Теги добавлены к задаче: важное
```

**Что проверить:**
- ✅ Теги добавлены к задаче в TickTick
- ✅ Существующие теги не удалены
- ✅ Сообщение понятное

**Альтернативные команды:**
- `Добавь теги срочно, важное к задаче купить молоко`
- `Добавь тег работа к задаче подготовить отчет`

---

### Тест 4: Добавление заметок (AC-9) ✅

**Команда:**
```
Добавь заметку к задаче купить молоко: не забыть взять сумку
```

**Ожидаемый результат:**
```
✓ Заметка добавлена к задаче 'купить молоко'
```

**Что проверить:**
- ✅ Заметка добавлена к задаче в TickTick
- ✅ Существующие заметки не удалены
- ✅ Сообщение понятное

**Альтернативные команды:**
- `Добавь заметку к задаче купить молоко: это важная задача`
- `Добавь описание к задаче купить молоко: нужно купить в магазине на углу`

---

### Тест 5: Удаление задачи (AC-4) ✅

**Команда:**
```
Удали задачу купить молоко
```

**Ожидаемый результат:**
```
✓ Задача 'купить молоко' удалена
```

**Что проверить:**
- ✅ Задача удалена из TickTick
- ✅ Задача удалена из кэша
- ✅ Сообщение понятное

**Проверка кэша:**
```bash
cat task_cache.json | jq . | grep "купить молоко"
# Должно быть пусто
```

---

### Тест 6: Перенос задачи между списками (AC-5) ✅

**Сначала создайте задачу:**
```
Создай задачу тестовая задача для переноса
```

**Затем перенесите:**
```
Перенеси задачу тестовая задача для переноса в список Работа
```

**Ожидаемый результат:**
```
✓ Задача перемещена в список <project_id>
```

**Что проверить:**
- ✅ Задача перемещена в указанный список
- ✅ Кэш обновлен с новым project_id
- ✅ Сообщение понятное

---

### Тест 7: Создание задачи с параметрами ✅

**Команда:**
```
Создай задачу подготовить отчет на завтра в списке Работа с тегом срочно
```

**Ожидаемый результат:**
```
✓ Задача 'подготовить отчет' создана в списке Работа
```

**Что проверить:**
- ✅ Задача создана с правильной датой
- ✅ Задача в правильном списке
- ✅ Тег добавлен
- ✅ Все параметры учтены

---

## Проверка логов

### Просмотр логов в реальном времени

```bash
tail -f /tmp/ticktick_bot.log
```

### Просмотр последних ошибок

```bash
grep -i error /tmp/ticktick_bot.log | tail -20
```

### Просмотр успешных операций

```bash
grep -i "task.*created\|task.*updated\|task.*deleted" /tmp/ticktick_bot.log | tail -20
```

---

## Ожидаемые результаты

### Успешные операции

Все операции должны:
- ✅ Выполняться в течение 3-5 секунд
- ✅ Показывать понятные сообщения
- ✅ Сохранять данные в TickTick
- ✅ Обновлять кэш корректно

### Обработка ошибок

Если возникает ошибка:
- ✅ Показывается понятное сообщение пользователю
- ✅ Ошибка логируется в `/tmp/ticktick_bot.log`
- ✅ Система не падает, продолжает работать

### Примеры ошибок и их обработка

**Ошибка: Задача не найдена**
```
Задача 'несуществующая задача' не найдена. Попробуйте создать новую задачу или укажите ID задачи.
```

**Ошибка: Не указаны параметры**
```
Не указаны параметры для обновления
```

---

## Чек-лист тестирования

### Базовые функции

- [ ] Создание задачи работает
- [ ] Редактирование задачи работает
- [ ] Удаление задачи работает
- [ ] Добавление тегов работает
- [ ] Добавление заметок работает
- [ ] Перенос задачи работает

### Проверка качества

- [ ] Сообщения понятные и информативные
- [ ] Время ответа < 5 секунд
- [ ] Ошибки обрабатываются корректно
- [ ] Кэш обновляется правильно
- [ ] Логи содержат полезную информацию

### Интеграция с TickTick

- [ ] Задачи создаются в TickTick
- [ ] Задачи обновляются в TickTick
- [ ] Задачи удаляются из TickTick
- [ ] Теги добавляются в TickTick
- [ ] Заметки добавляются в TickTick

---

## Дополнительные тесты

### Тест 8: Массовые операции (AC-6)

**Команда:**
```
Перенеси все просроченные задачи со вчера на сегодня
```

**Ожидаемый результат:**
```
✓ Перенесено 0 задач (или количество задач)
```

**Примечание:** Может вернуть 0, если GET endpoint TickTick API не работает.

---

### Тест 9: Повторяющиеся задачи (AC-10)

**Команда:**
```
Создай повторяющуюся задачу позвонить маме ежедневно
```

**Ожидаемый результат:**
```
✓ Задача 'позвонить маме' создана с повторением
```

---

### Тест 10: Аналитика (AC-15)

**Команда:**
```
Сколько за последнюю неделю было рабочего времени
```

**Ожидаемый результат:**
```
📊 Аналитика за последнюю неделю:
Рабочее время: X часов
```

**Примечание:** Может вернуть 0, если GET endpoint TickTick API не работает.

---

## Устранение неполадок

### Проблема: Веб-сервер не запускается

**Решение:**
```bash
# Проверьте, не занят ли порт 8000
lsof -i :8000

# Убейте процесс, если нужно
kill -9 <PID>

# Запустите снова
python3 -m uvicorn src.web.main:app --host 127.0.0.1 --port 8000
```

### Проблема: Ошибки аутентификации

**Решение:**
1. Проверьте `docs/credentials.md` или `.env`
2. Убедитесь, что токены правильные
3. Проверьте логи: `tail -f /tmp/ticktick_bot.log`

### Проблема: Задачи не находятся

**Решение:**
1. Проверьте кэш: `cat task_cache.json`
2. Убедитесь, что задача была создана через бота
3. Проверьте логи на наличие ошибок

### Проблема: TickTick API возвращает ошибки

**Решение:**
- Проверьте, что токен TickTick правильный
- Проверьте документацию TickTick API
- Некоторые endpoints могут не работать (это нормально)

---

## Контакты и поддержка

Если возникли проблемы:
1. Проверьте логи: `/tmp/ticktick_bot.log`
2. Проверьте документацию: `docs/`
3. Проверьте тесты: `tests/`

---

**Удачного тестирования! 🚀**

